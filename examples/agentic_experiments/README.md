# Agentic Experiment Inputs

Saved planner outputs go here:

```text
examples/agentic_experiments/{benchmark}/{mode}/model_output.json
```

Use `docs/agentic_layout/experiment_protocol.md` for the full manual and optional-provider workflows.
